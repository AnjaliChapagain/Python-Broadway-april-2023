# git init
# git add . (git add --all)
# git commit -m "commit_message>"  ---- determines who wrote the code
# git remote add origin <ssh link>
# git push -u origin <branch_name>


# git status # Shows the current status of all git staged and unstaged files

# setting git configuration  -- don't have to do this everytime, already done
# 1. git config --global user.name "<username>"
# 2. git config --global user.email "<your-email>"

# Generating ssh key pair ----don't have to do this everytime, already done
# ssh-keygen -t rsa -b 4096 -C "your_email@example.com"




##### Git Branching

# git branch # It shows all the branches and highlighjts the current branch

#git branch <branch_name>    # Creates a new branch
# git checkout <branch_name>  # jumps ( checkout) to the next branch

# git checkout -b <branch_name>  # Creates a new branch and jumps to that branch

